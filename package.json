{
  "name": "lexora-vocabulary-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "A personal SAT and IELTS vocabulary learning workspace.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check reading-lab.js && node --check api/reading-lookup.js && node --check server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": { "node": ">=22" }
}
