/*
 * Paste the Project URL and publishable (or legacy anon) key from Supabase here.
 * These values identify the public web client; they are not administrative secrets.
 * Never put a service_role key in this file or in browser code.
 */
window.LEXORA_SUPABASE_CONFIG = {
  url: "",
  anonKey: "",
};
